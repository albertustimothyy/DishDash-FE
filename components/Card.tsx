import { View, Text } from "react-native";
import React from "react";

const Card = () => {
  return (
    <View className="shadow-xl p-3 bg-red-600 rounded-lg">
      <Text className="text-3xl">Cards</Text>
    </View>
  );
};

export default Card;
